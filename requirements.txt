# Dependencias base: sólo tkinter (viene con Python en Windows).
#
# Opcional, sólo para la Vista Previa embebida (Ver > Vista Previa):
opencv-python-headless
pillow

# Opcional, sólo para generar el informe de Hashear en PDF (si no está
# instalado, igual se puede generar el informe en TXT). Si falta, la
# app ahora ofrece instalarlo sola con pip desde el diálogo de Hashear:
reportlab

# Además, ffmpeg y ffprobe deben estar disponibles en el PATH del
# sistema (no se instalan por pip). En Windows, si no están, la app
# ofrece descargarlos e instalarlos automáticamente al iniciar (o
# desde el menú Ayuda > "Instalar / verificar ffmpeg...").
